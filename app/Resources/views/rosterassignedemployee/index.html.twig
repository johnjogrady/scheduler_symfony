{% extends 'base.html.twig' %}

{% block body %}
    <h1>Rosterassignedemployees list</h1>

    <table>
        <thead>
        <tr>
            <th>Id</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for rosterAssignedEmployee in rosterAssignedEmployees %}
            <tr>
                <td>
                    <a href="{{ path('rosterassignedemployee_show', { 'id': rosterAssignedEmployee.id }) }}">{{ rosterAssignedEmployee.id }}</a>
                </td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('rosterassignedemployee_show', { 'id': rosterAssignedEmployee.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('rosterassignedemployee_edit', { 'id': rosterAssignedEmployee.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('rosterassignedemployee_new') }}">Create a new rosterAssignedEmployee</a>
        </li>
    </ul>
{% endblock %}
