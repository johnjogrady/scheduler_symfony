{% extends '_base.html.twig' %}

{% block body %}
    {{ dump() }}
    <h1>Employee Unavailability Time Creation</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Create"/>
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('employee_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
